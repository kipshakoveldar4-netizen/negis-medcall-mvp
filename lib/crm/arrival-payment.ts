/** Only allowlisted database codes become client-facing explanations. */
export function arrivalPaymentError(error: { code?: string }): string | null {
  const messages: Record<string, string> = {
    P6101: "Проверьте связь записи, клиента и услуги с текущей клиникой.",
    P6102: "Продажа по этой записи уже существует. Откройте её в разделе продаж.",
    P6103: "По записи найдено несколько продаж. Проверьте их перед подтверждением прихода.",
    P6104: "Продажа по записи отменена или возвращена. Проверьте оплату в разделе продаж.",
    P6105: "Укажите стоимость услуг в записи перед подтверждением прихода.",
    P6106: "Валюта существующей продажи отличается от валюты записи. Проверьте продажу.",
    P6107: "Нельзя отвязать продажу, оформленную при подтверждении прихода.",
  };
  return messages[error.code ?? ""] ?? null;
}
