# Сайт и блог Medina OS

## Согласованные решения

- Сайт внутри текущего проекта, без второй CRM и базы клиентов.
- Бренд Medina OS; аудитория: салоны, стоматологии и клиники.
- Сначала русский язык, затем казахский.
- Основной призыв: «Оставить заявку», не WhatsApp.
- Отдельное пространство «Medina OS — маркетинг», владелец eldar@mail.ru.
- medinaos.kz — кандидат, доступность и покупка пока не подтверждены.
- Демонстрационные кейсы только в закрытом предпросмотре с явной пометкой;
  публичные результаты, отзывы и кейсы требуют подтверждения владельца.

## Подготовка пространства

В Medina Control форма подключения поддерживает назначение «Маркетинг и
привлечение клиентов». Оно хранится в `workspace_settings` под ключом
`workspace_purpose`, значение `{ "purpose": "marketing" }`. Новая таблица
или миграция не нужна. Это назначение CRM, не рекламная ниша и не право доступа.

Для этого назначения доступно только приглашение: существующий пользователь
принимает его своим аккаунтом. Пароль и существующие членства не меняются.
Создание остаётся за `requirePlatformOwner`; владелец клиники самовольно
создать пространство через этот маршрут не может. Дополнительное пространство
для одной почты требует существующего явного подтверждения. Публичная
саморегистрация не включается.

Маркетинговому пространству не записывается выдуманная медицинская ниша.
Существующая рекламная политика НЕ ослабляется: пока специальная рекламная
политика не реализована, отсутствие ниши оставляет консервативную проверку.
Старые CRM-экраны ещё могут показывать медицинские подписи по умолчанию.
Назначение само по себе не подключает сайт, тариф, форму заявок или рекламу.
Поддержка опубликована в `ed976b5`. Пространство создано через production
Medina Control. По отдельному разрешению владельца существующий подтверждённый
аккаунт eldar@mail.ru привязан напрямую как активный owner; неиспользованное
приглашение отозвано. Вход и админ-доступ проверены в браузере для пространства
1556034a-7a67-4ceb-b3c9-9748e53e588f. Пароли и членства других пространств не менялись.
Токен приглашения не сохраняется в документации.

## Локальный предпросмотр сайта

`artifacts/medina-site` содержит статический HTML-генератор без новых зависимостей:
главную, четыре услуги, список блога, два редакционных черновика и HTTP 404.
Используется существующий логотип Medina OS. Новые API, таблицы и настройки
деплоя не добавлены; маршруты входа и CRM не изменены.

Запуск: `node artifacts/medina-site/dev.mjs` (127.0.0.1:5186/ru/).
Сборка: `node artifacts/medina-site/build.mjs`.
Проверки: `node --test artifacts/medina-site/site.test.mjs`.

Это ещё не опубликованный сайт и не CMS. Индексация закрыта через robots и
метаданные; локальный сервер также возвращает X-Robots-Tag. Форма полностью
отключена и не передаёт данные. Перед публикацией нужны защищённый приём заявок,
согласие на обработку данных, редакционная проверка, домен и SEO-настройки.
Управление статьями из кабинета, canonical и sitemap пока не реализованы.

## Основа приёма заявок (058, ещё не включена)

Миграция `058_public_site_intake_foundation.sql` добавляет только серверные
таблицы `crm_intake_sites` и `crm_site_inquiries` и RPC `accept_crm_site_inquiry`.
Не создаёт привязок сайта, не включает приём и не меняет существующие записи.
По умолчанию сайт выключен. Применение миграции само по себе форму не оживляет.

Пространство определяется только по серверной привязке `site_key`. В форме нет
workspaceId, clientId, leadId, рекламных идентификаторов или произвольных полей.
Допустимы имя, телефон, организация, одно из четырёх направлений, путь страницы
без query/hash и явное согласие с версией текста. Телефон нормализуется общим
CRM helper и отдельно проверяется. Медицинские сведения форма не собирает.

Серверная функция проверяет страницу и версию согласия по настройке сайта.
Под блокировкой строки сайта она сохраняет заявку CRM и подтверждение согласия
в одной транзакции. Повтор с тем же request_key возвращает тот же безопасный
ответ; изменение данных под этим ключом отклоняется. Повтор с новым ключом и
точно теми же именем, телефоном, организацией и услугой в течение суток может
использовать ту же заявку сайта. Исторические CRM-заявки не перезаписываются.
Источник «Сайт» и начальный этап берутся из справочников своего пространства.

Ограничения по умолчанию: 100 новых отправок на сайт за час, 5 на телефон за
сутки. Это транзакционные ограничения хранения, **не полноценная антибот-защита**.
Точный повтор уже сохранённого запроса лимит повторно не расходует.
Персональные данные находятся в закрытой таблице согласий и в CRM, не в ответе.
RPC возвращает только `{ accepted: true }`, без контактов и внутренних ID.
RLS включён, anon/authenticated не могут читать таблицы или вызывать RPC.
Вызов предназначен только для серверного service_role.

До публичного подключения обязательны:
- ограниченный по размеру серверный адаптер внутри существующего API;
- проверенная серверная привязка домена/сайта (не tenant из браузера);
- антибот-проверка и ограничение запросов на внешней границе;
- согласованный текст согласия, версия и путь политики;
- безопасные ответы без ошибок БД, контактов и токенов;
- учёт `crm_site_inquiries` в удалении персональных данных и политике хранения;
- проверка формы и доставки в нужное пространство, затем отдельное включение.

Изолированный маршрут описан ниже; обычные CRM-ресурсы по-прежнему требуют
авторизации, старый webhook остаётся закрытым. UTM и аналитика отложены до отдельной
санитизации; полные URL и произвольные payload не сохраняются.

Тесты: `pnpm --filter @workspace/scripts run test:site-intake` выполняют SQL
в изолированном PGlite и проверяют права, изоляцию пространств, согласие,
повторы, лимиты и транзакционный откат. Production этими тестами не затрагивается.
PGlite не заменяет будущую проверку параллельных транзакций в PostgreSQL.

## Подключение формы (код готов, включение отдельно)

`POST /api/crm/site-inquiry` внутри существующего catch-all принимает только
requestKey UUID v4, inquiry и challengeToken. Нет GET со сведениями о заявках,
нет выбора workspace/site посетителем. Путь с дополнительными сегментами — 404.
Настройки отсутствуют — 503, без обращения к провайдеру или БД.
Тело ограничено 8 КБ до разбора JSON. Верифицируются точный Origin, одноразовый
Turnstile token, hostname и action `site_inquiry`. Ошибка проверки закрывает
запись. Права владельцев CRM и HMAC работников не заменяются этим механизмом.

Серверные переменные в защищённых настройках API deployment (не в браузере):
- `MEDINA_SITE_INTAKE_ENABLED=true` — только после всех проверок готовности;
- `MEDINA_SITE_ORIGIN` — точный HTTPS origin сайта без пути/слэша;
- `MEDINA_SITE_INTAKE_KEY` — ключ включённой строки crm_intake_sites;
- `MEDINA_SITE_TURNSTILE_SECRET` — секрет Turnstile; не присылать в чат;
- существующие Supabase server env.

Публичные переменные **сборки сайта**, не секреты:
- `MEDINA_SITE_FORM_ENABLED=true`;
- `MEDINA_SITE_API_ORIGIN` — HTTPS origin CRM API;
- `MEDINA_SITE_TURNSTILE_SITE_KEY` — публичный site key виджета;
- `MEDINA_SITE_CONSENT_VERSION` — версия, совпадающая с настройкой сайта.

Без явного включения сборка сохраняет отключённую форму и не загружает
Cloudflare. При включении посетитель подтверждает согласие, получает challenge,
затем запрос передаётся прямо API без cookie/пользовательского Bearer.
Секрет, имя и телефон не отправляются друг другу: Cloudflare получает только
challenge token и серверный secret, без содержимого заявки и явного remoteip.
Сам виджет является сторонним сервисом; это необходимо отразить в политике.
Проверка описана в [официальной документации Turnstile](https://developers.cloudflare.com/turnstile/get-started/server-side-validation/).

Повтор после сетевой ошибки сохраняет requestKey до изменения полей и требует
свежего challenge. Успех показывается только после HTTP success и `success:true`.
Ошибка БД или пустой ответ не превращаются в успех. Контакты остаются только
в памяти формы до ухода со страницы; localStorage и URL для них не используются.

Перед включением требуется применить 058, отдельно настроить строку сайта на
нужное пространство с разрешёнными страницами/версией согласия, настроить
Turnstile только на подтверждённый домен и edge/WAF rate limiting.
60 проверок в минуту на экземпляр API — лишь дополнительный локальный
предохранитель, не распределённая защита. Лимиты базы сохраняются независимо.
Нужно также проверить удаление согласий по запросу владельца данных и текст
политики, после чего провести согласованную проверку доставки тестовой заявки.
Ничего из этого не применяется автоматически этим коммитом.

Файлы сборки остаются `noindex`: отдельный hosting/deployment и индексация
пока не подключены. При отдельном hosting перенести CSP из dev.mjs в headers
deployment: скрипты/iframe только self и challenges.cloudflare.com, connect-src
дополнительно точный API origin, form-action none. Не разрешать wildcard.
Секретов Turnstile в тестах нет: провайдер и записи БД подменяются тестовыми
реализациями, а SQL отдельно выполняется в PGlite.

## Закрытый редактор черновиков

Раздел `/site-blog` («Сайт и блог») позволяет владельцу/администратору
создавать статьи, сохранять черновики и смотреть закрытый предпросмотр.
Сервер проверяет сессию, роль и пространство; переключатель режима интерфейса
не предоставляет доступ. Ресурс находится в существующем CRM catch-all.

Для хранения нужна миграция `059_site_blog_drafts.sql`. Она создаёт закрытую
таблицу `site_blog_posts`; прямой доступ anon/authenticated запрещён.
Миграция 059 применена пользователем; сохранение и повторное открытие первого
черновика проверены в production в маркетинговом пространстве 25.09.2026.
Редактор развёрнут в `3cbc4a6`. О применении 058 сообщил пользователь;
это не означает автоматического включения публичной формы.

Текст хранится как обычный текст, не исполняемый HTML. Черновики не пишутся
в localStorage и не подменяются демонстрационными данными при ошибках.
Версия записи предотвращает перезапись одновременных изменений.
Повтор создания после потери ответа использует тот же идентификатор.

Этап публикации описан ниже. Категории и загрузка изображений ещё не подключены.
Статические примеры не импортируются автоматически. Предпросмотр не делает
черновик доступным посетителям сайта.

Проверка: `pnpm --filter @workspace/scripts run test:site-blog`.
Тесты выполняют миграцию и запросы обработчика в изолированном PGlite,
проверяют роли, пространства, повтор запроса, конфликт версий и пагинацию.

## Подтверждённые версии статей (060)

Миграция `060_site_blog_publication.sql` добавляет снимок опубликованной версии,
дату и номер версии. Старые статьи остаются закрытыми; миграция не публикует
их автоматически. `set_site_blog_publication` разрешена только service_role.
HTTP-обработчик сначала проверяет сессию и роль owner/admin в пространстве.
RPC блокирует строку и проверяет версию; чужая или устаревшая запись не меняется.
Пустой текст/описание и конфликт публичных адресов отклоняются.

В редакторе действия «Разрешить публикацию», «Обновить публикацию» и
«Снять с публикации» требуют явного подтверждения. При несохранённых правках
они отключены. Обычное сохранение не меняет публичный снимок. Снятие очищает
снимок, но не удаляет черновик. Публичный обработчик не кэширует HTML, поэтому
следующий запрос перестаёт показывать снятую статью; поисковые копии и уже
открытые вкладки этим не удаляются.

До применения 060 обычные черновики продолжают работать, а действие публикации
возвращает понятное сообщение о неподключённом хранилище. Пользователь сообщил
о применении 060; отдельную проверку на живой базе не проводили.

## Публичный сайт в том же deployment

Новый serverless-файл не нужен. `/ru/`, четыре страницы услуг, `/ru/blog/`,
статьи и `/ru/sitemap.xml` переписываются в отдельный GET/HEAD-обработчик
`site-page` существующего catch-all. Корневые маршруты CRM не перенаправляются.
Отдельная конфигурация вида `site_public` НЕ открывает другие CRM-ресурсы.
Посетитель не может выбрать workspaceId. Используется только пространство
из серверных настроек; наружу идут разрешённые title/slug/excerpt/body снимка.
Нет ID, сотрудников, заявок, секретов, приватных версий или примеров-кейсов.

Серверные переменные (включать только после применения 060):
- `MEDINA_PUBLIC_SITE_ENABLED=true` — отдельный флаг публичного чтения;
- `MEDINA_PUBLIC_SITE_WORKSPACE_ID` — UUID маркетингового пространства;
- `MEDINA_SITE_ORIGIN` — подтверждённый HTTPS origin без пути;
- `MEDINA_SITE_INDEXABLE=true` — только после проверки текстов и готовности сайта.

Без этих настроек страницы закрыты. При ошибке БД — 503, не пустой успешный блог.
В MVP каталог ограничен 200 опубликованными статьями; превышение даёт явный
503 вместо тихого усечения sitemap. Перед ростом каталога нужна пагинация.
Черновики в этот предел не входят.

HTML рендерится на сервере, текст экранирован. Есть description, Open Graph,
canonical, sitemap; удалённая/несуществующая статья даёт HTTP 404 и noindex.
Обычные UTM/click-id в адресе страницы допускаются с ограничением длины, но
игнорируются: не сохраняются, не отражаются в HTML/canonical и не передаются CRM.
`/robots.txt` разрешает только `/ru/` при отдельном включении индексации.
Пока сайт выключен, robots-обработчик возвращает 404 с noindex, без сведений CRM.
Казахский язык пока НЕ заявлен: нет выдуманного перевода и hreflang на пустые страницы.

`copy-assets.mjs` переносит только CSS, форму и логотип в сборку CRM;
приватные документы, SQL и черновики не копируются. Он вызывается в Vercel после
сборки frontend. Отдельный домен не требуется для технической проверки `/ru/`;
выбор и подключение домена остаются отдельным этапом.

Форма включается только при одновременной готовности:
`MEDINA_SITE_FORM_ENABLED`, серверной intake-конфигурации, публичного Turnstile
site key и записи `crm_intake_sites` с тем же workspace, enabled=true,
разрешённым `/ru/` и версией согласия. Секрет никогда не включается в HTML.
До настройки Turnstile/WAF и согласования политики форма остаётся отключённой.
Ключи не присылать в переписку. Реальная доставка заявки пока не проверена.

## Статус плана и внешние зависимости

- Готово: маркетинговое пространство, закрытый редактор, черновики и предпросмотр.
- Реализовано локально, ожидает отдельного preview и согласования выпуска: подтверждённые версии и снятие публикации,
  серверный HTML, metadata, sitemap, 404, изоляция пространства сайта.
- Реализовано, ожидает настроек/проверки доставки: защищённая форма заявок.
- Не готово: окончательный домен, Turnstile и WAF, согласованный срок хранения
  и проверка удаления согласий, реальные кейсы, категории/медиа, казахский язык.
- Следующий отдельный этап: сайты клиник и их собственные привязки, без общей
  базы контактов и без автоматической публикации клинических данных.

## Дальнейшие этапы

1. Завершить операторский цикл: приход клиникой, контрольный звонок,
   общая защита от одновременных записей сотрудником и оператором.
2. Пространство и членство владельца готовы; подключить серверное назначение сайта.
3. Реализовать публичный индексируемый HTML: главная, услуги, форма заявки.
4. Добавить «Сайт и блог»: шаблоны, черновик, закрытый предпросмотр,
   публикация/снятие с публикации, категории и проверенные кейсы.
5. Метаданные, canonical, sitemap, robots, настоящие HTTP 404, RU/KZ маршруты.
6. Подключить сайтам клиник собственные назначения заявок, не общую базу Medina.

Приём форм должен определять пространство по серверной привязке сайта, а не
доверять workspaceId посетителя. Переиспользовать нормализацию телефона и
существующую обработку заявок, добавить защиту от спама/повторов и сохранять
согласие, страницу и допустимые UTM. Старый webhook со статусом 410 не открывать.
Meta Pixel сейчас отключён; события и согласия согласовать до включения.
Не передавать персональные/медицинские данные в аналитику по умолчанию.

Проверка текущего изменения: `pnpm --filter @workspace/scripts run test:onboarding`.

## Восстановление после сбоя 25 сентября 2026

### Название продукта

Видимое название — Medina OS, включая CRM, юридические страницы, рекламные
подсказки и уведомления. Проверка `pnpm run test:branding` не допускает старое
название в интерфейсе и проверяет совместимость источников старых записей.
Ключи сессий `negis_*`, CSS-переменные, URL интеграций и значения источников в
БД не переименовываются. Это не миграция данных и не массовое завершение сессий.

### Следующий контрольный этап

Уточнение владельца от 26 сентября: новые облачные проекты не создаём.
Работа продолжается в текущем репозитории; испытания остаются локальными,
с вымышленными данными. Описанная ниже отдельная облачная среда не создана.
Действующие клиники и салоны не заменяют её. Публикация в production и
изменение настроек рабочих пространств требуют отдельного разрешения.

До выпуска нужен отдельный preview без production-секретов и с отдельной
тестовой Supabase-базой. Одного нового workspace в живой базе недостаточно.
В этой среде проверить вход владельца, администратора и мастера, обновление
страницы, чтение записей, сохранение/публикацию/снятие тестовой статьи и 404
после снятия. Публичная форма до отдельной проверки защиты остаётся выключенной.
Затем представить результаты пользователю и запросить согласование production.

Релиз e6de5ff был отозван коммитом 64a26f7: CommonJS-функция CRM на Vercel
пыталась загрузить ESM render.mjs через require. Это ломало запуск всей общей
функции, включая auth-context. Успешная проверка типов не выявляла ошибку среды.

Локальное исправление: сервер загружает render.cjs и content.cjs. Прежние .mjs
остались тонкими обёртками для локального предпросмотра; копии шаблонов нет.
`test:crm-cold-start` компилирует api/lib в CommonJS и в отдельном процессе
запускает настоящий catch-all без секретов, с запрещённой сетью. Он проверяет
401 на закрытых маршрутах и 404 выключенного сайта. Проверка включена в build
и в конфигурацию сборки Vercel. Это не заменяет проверку отдельного Vercel preview.

Пользователь сообщил о применении 060. Производственная БД повторно не изменялась
и не использовалась для тестов. Публикация и снятие проверены только в локальной
PGlite. Никакие статьи автоматически не публикуются.

Следующий выпуск требует отдельного тестового развёртывания и тестовой базы,
проверки входа владельца/администратора/мастера, затем явного согласования.
Не использовать действующие клиники, салоны или их данные. Не отправлять текущую
работу в main и не включать публичный сайт без такого согласования.

### Локальный контроль доступа перед отдельным preview

Проверка `test:crm-cold-start` теперь выполняет собранный CommonJS catch-all
с полностью вымышленными сессиями owner/admin/doctor и локальной подменой Auth
и БД. Проверяются входной контекст, пустые записи без demo-подмешивания,
доступ редактора только владельцу/администратору, отказ другому пространству,
просроченному токену и отключённому сотруднику, закрытый отказ при сбое членства.
Публичный HTML проверяется и при включённом локальном флаге: разрешённый снимок
открывается, черновик и новые неопубликованные правки не раскрываются, текст
экранируется, без опубликованного снимка возвращается 404. Посетитель не выбирает
клинику через query-параметр. Реальная сеть и production-переменные недоступны.

Это проверка серверного выполнения и прав, **не проверка настоящего входа
в браузере, сессий Supabase или конфигурации Vercel**. Для следующего контрольного
этапа по-прежнему нужны отдельная тестовая Supabase-база и preview-развёртывание.
Их готовность ещё не подтверждена. Действующие клиники для тестов не используются.

### Изоляция сбоя публичного шаблона

Публичный `site-page` загружается только при обращении к зарегистрированному
публичному маршруту. Ошибка импорта или выполнения возвращает нейтральный 503,
`no-store` и `noindex`, без текста исключения. У HEAD нет тела ответа.
Приватные маршруты по-прежнему проходят прежнюю серверную авторизацию.

Cold-start тест выполняется в двух отдельных процессах: обычная сборка и
принудительный отказ загрузки `render.cjs`. Во втором случае сайт недоступен,
но проверенные сессии владельца/администратора/мастера и записи продолжают
обслуживаться, в том числе после неудачного запроса сайта в том же процессе.
Это защита от класса предыдущего сбоя шаблона, не гарантия отсутствия любых
других ошибок общей CRM-функции. Исходники и данные production не меняются тестом.
